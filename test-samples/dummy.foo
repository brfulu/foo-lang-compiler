import 'op';

int a;
a = 3;

func
    .name('asd')
    .params(int a)
    .returns(int)
    .body(
        int c = 4;
    );

if
    .cond(a < 3)
    .then(
         3 + 2;
    )
    .else(
        int d = 3 * 2;
    );