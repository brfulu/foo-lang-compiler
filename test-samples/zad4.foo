import 'io';

int n = io.in.call();
flt sum = 0;

loop
  .init(
    int i = 0;
  )
  .cond(i < n)
  .body(
    int x = io.in.call();
    sum += x;
    ++i;
  );

flt avg = sum / n;
io.out.call('Average:', avg);